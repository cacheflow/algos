/**
 * @param {number} num
 * @return {number}
 */
var maximumSwap = function(num) {
  num = num.toString().split().map(n => )
  let min = num[0]
  for(var i = 0; i < num.length; i++) {
    console.log(num)
  }
  console.log(min)
};

maximumSwap(2736)
